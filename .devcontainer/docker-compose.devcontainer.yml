version: "3.9"

services:
  pifi:
    # Dev Containers will set HOME and mount /home/dev via devcontainer.json mounts.
    # Keep this override minimal to avoid conflicting mounts.
    environment:
      FIRST_RUN: "false"
